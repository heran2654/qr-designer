# QR Designer V1 产品设计文档

## 1. 产品定义

QR Designer 是一个“高自由度 + 强防呆”的本地可视化二维码设计器。用户可以自由修改二维码视觉效果；任何会明显降低识别可靠性的参数，都由 Safety Engine 通过动态安全范围限制。

核心原则：

1. 不用频繁弹窗打断设计；
2. 参数理论范围始终完整显示；
3. 当前不可用区间用红色轨道表达并禁止进入；
4. 参数变化后用真实解码验证，而不是展示含义模糊的百分制评分；
5. 用户主动调整的参数优先，其他参数的安全范围随之收缩；
6. 无法可靠识别时禁止导出，并提供最小改动的自动修复。

## 2. 产品目标

- 支持文本、URL、中文/Unicode 和任意字符串；
- 让用户能够设计模块、定位点、颜色、渐变、图片纹理与 Logo；
- 在编辑过程中持续给出可信的识别状态；
- 导出前通过独立解码器与压力测试；
- 纯本地、无账号、无上传、低 CPU 和低内存占用。

## 3. 总体架构

```text
内容输入
  ↓
QR 编码核心（Version / ECC / 容量）
  ↓
标准二维码矩阵
  ↓
功能区域分类（Finder / Alignment / Timing / Format / Data）
  ↓
样式渲染器（模块 / 颜色 / 渐变 / 纹理 / Logo）
  ↓
Safety Engine
  ├─ 参数安全范围计算
  ├─ 主动参数优先约束
  ├─ 结构与对比度检查
  ├─ jsQR 快速预检
  ├─ ZXing 独立复检
  └─ 缩放 / 模糊压力测试
  ↓
实时画布
  ↓
导出验证
  ↓
PNG / SVG
```

## 4. 信息架构

桌面界面采用“顶部状态与动作 + 左侧一级导航 + 中央画布 + 右侧参数面板 + 底部技术状态栏”。

一级导航固定为：

1. 内容
2. 二维码
3. 定位点
4. 颜色
5. Logo
6. 背景
7. 导出

中央画布始终保持最大可用面积，右侧面板滚动不影响预览尺寸。

## 5. 动态安全滑块

### 5.1 视觉语义

- 主题色：当前值与已选择区间；
- 中性轨道：当前可调范围；
- 红色斜纹：当前禁止范围；
- 圆形 Handle：当前值；
- 红区仍保留完整理论刻度。

### 5.2 行为

- 拖动进入红区时，Handle 停在安全边界；
- 直接输入越界数值时，自动压回边界并显示短暂 Toast；
- 悬停红区显示安全上/下限与主要限制因素；
- 一个参数变化后，相关滑块的红区实时重新计算；
- 当前正在拖动的参数为主动参数，Safety Engine 优先锁定它，再调整其他参数范围；
- 只有主动参数到达绝对边界时才阻止继续拖动。

### 5.3 主要联动

- Logo 大小 ↔ Logo 空隙 ↔ 模块间距 ↔ 纠错等级；
- 模块大小 ↔ 模块间距 ↔ QR Version；
- 透明度 ↔ 前景/背景对比度；
- 图片纹理强度 ↔ 图片对比度 ↔ 暗部保护；
- 定位点圆角 ↔ 外框尺寸 ↔ 中心尺寸。

Logo 间隙不使用连续百分比，而使用二维码自身的 module 作为单位。滑块按 `0、1、2…` 个 module 调整，并提供“保持 1 个模块间隙”开关。启用后每侧固定留出一个完整 module，滑块保留显示但锁定。

## 6. 功能面板

### 6.1 内容

- 内容输入；
- 自动内容类型；
- L/M/Q/H 纠错等级；
- 自动或固定 Version 1—40；
- UTF-8 字节数、当前版本容量与容量进度；
- 超容量时停止生成并禁止导出。

### 6.2 普通模块

- 方形、圆形、圆角；
- 圆角、模块大小、模块间距；
- 圆形模式隐藏无意义的圆角参数；
- Timing、Format、Version 等结构区域受保护。

### 6.3 定位点

- 三个定位点默认同步；
- 外框与中心分别控制；
- 支持尺寸、圆角和中心比例；
- 解除同步后可切换左上、右上、左下分别设置；
- Alignment Pattern 位于二维码高级设置，可跟随定位点、普通模块或自定义。

### 6.4 颜色与纹理

- 单色；
- 线性、径向、角度渐变；
- 渐变起止色与方向；
- 图片纹理导入、强度与对比度；
- 前景/背景组合实时对比度检查；
- 无论怎样调整都无法达到最低对比度时禁止导出。

### 6.5 Logo

- 导入 PNG/JPG/WebP/SVG；
- 内置占位 Logo；
- Logo 大小、整数 module 周围空隙与圆角；
- 浅色背景自动移除；
- 隐藏/显示；
- Canvas 控制点直接缩放，并同步右侧滑块；
- Logo Mask、Safe Area 与功能模块保护共同决定挖空范围；
- Logo 与安全区吸附到模块网格，只挖掉完整 module，不绘制连续背景色遮盖块；
- 每次 Logo 尺寸或间隙稳定后，重新生成 8 个标准 QR Mask Pattern，选择挖空区内深色数据模块最少的合法矩阵。

### 6.6 背景

- 纯色或透明；
- 透明模式在编辑画布显示棋盘格；
- 识别验证时使用白色目标背景模拟；
- 目标背景模拟不进入导出文件。

### 6.7 导出

- PNG / SVG；
- 512、1024、2048、4096 预设；
- 256—8192 自定义正方形尺寸；
- 二维码结构、ZXing、jsQR、缩放/模糊、对比度检查；
- 任一强制检查失败时按钮置灰；
- 图片纹理选择 SVG 时自动改用 PNG，避免伪矢量和纹理丢失。

## 7. 画布

- 50%—200% 连续缩放；
- 适应窗口；
- 模块网格开关；
- Logo 安全区开关；
- Logo 右下控制点直接缩放；
- 所有辅助层只存在于编辑界面，不进入导出。

## 8. 识别状态

只展示三种可理解状态：

- 正在验证；
- 可正常识别；
- 当前不可导出。

底部状态栏展示实际 Version、纠错等级、模块数量、内容字节数以及两个解码器的结果。产品不展示“87 分”之类线性含义不成立的安全评分。

## 9. 自动修复

修复顺序遵循“最少改变用户设计”：

1. 缩小 Logo 或安全空隙；
2. 减少模块间距；
3. 增大模块尺寸；
4. 提高透明度；
5. 只有对比度确实不足时才恢复高对比度颜色。

内容、纠错等级、Logo 文件和整体风格优先保留。修复后用 Toast 列出实际变更，并重新执行识别验证。

## 10. 低占用决策

- 无 UI 框架、无构建产物、无后台进程；
- 本地编码器约 57 KB；
- jsQR 约 251 KB；
- ZXing 浏览器构建约 431 KB，并在首次验证时加载；
- 参数输入做 360 ms 防抖，避免拖动过程中持续解码；
- 压力测试只在深度检查或导出时执行。

OpenCV.js 官方浏览器文件约 10.5 MB，标准 Web 构建也未暴露可直接使用的 `QRCodeDetector`。V1 使用 ZXing + jsQR 两个独立实现替代，在保持双引擎验证的同时显著降低体积、解析时间和内存占用。

## 11. V1 验收标准

1. 默认设计必须同时通过 ZXing、jsQR、缩放、轻微模糊和对比度检查；
2. 红区不可通过拖动或数值输入绕过；
3. 内容、ECC、Version 或样式变化后，预览与状态必须更新；
4. 失败设计必须禁止导出并给出可操作原因；
5. 自动修复后必须重新验证；
6. PNG 与 SVG 必须包含同一内容和主要视觉样式；
7. 透明导出不得包含画布棋盘格或编辑辅助层；
8. 应用可从本地文件直接启动，不依赖网络或常驻服务。
